# Claude API Configuration
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# MCP Server Configuration
# IMPORTANT: Update these paths to match your actual installation directory
# The MCP server needs to run with Python that has FastMCP installed
# Option 1: Use project root .venv (recommended if FastMCP is installed there)
MCP_SERVER_COMMAND=/absolute/path/to/project/.venv/Scripts/python.exe
MCP_SERVER_ARGS=/absolute/path/to/project/mcp-server/server.py
# Option 2: Use relative paths if your PATH has correct Python (not recommended)
# MCP_SERVER_COMMAND=python
# MCP_SERVER_ARGS=../mcp-server/server.py

# API Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
API_HOST=0.0.0.0
API_PORT=8000

# Claude Model Configuration
CLAUDE_MODEL=claude-sonnet-4-20250514
CLAUDE_MAX_TOKENS=4096

# Note: Trello credentials are configured in ../mcp-server/.env
# The MCP server manages its own Trello API access
